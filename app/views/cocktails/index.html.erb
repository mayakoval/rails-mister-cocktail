<h1>Fancy a drink?</h1>
<%= link_to 'Add a new cocktail', new_cocktail_path, class: 'btn btn-light' %>
<ul>
   <% @cocktails.each do |cocktail| %>
   <li><%= cocktail.name %></li>
   <td><%= link_to 'Show', cocktail %></td>
   <% end %>
</ul>
